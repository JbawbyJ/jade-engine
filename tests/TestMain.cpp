// Doctest entry point. This TU only generates main() so the actual test files
// stay lean — same split as keeping a jest.config apart from the specs.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
